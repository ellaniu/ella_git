#!/bin/bash
#thare are some comments as the following:
# 1. use ${array_name[@]} to get all elements of array_name
# 2. use ${#array_name[@]} or ${#array_name[*]} to get the length of this array_name
# 3. use ${#array_name[]n} to get the length of each element for this array_name
# 4. use # to comment each row

echo "hello world!"
